class Node:
    def __init__(self, value):
        self.value = value
        self.left = None
        self.right = None

# Initialise a binary tree with just a root node
# root = Node(0)

# Add a left node to the root node
# root.left = Node(1)